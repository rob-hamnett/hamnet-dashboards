apiVersion: 1

providers:
  - name: "Default workstation dashboard"
    orgId: 1
    folder: "Default"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards   # <-- the path inside the container